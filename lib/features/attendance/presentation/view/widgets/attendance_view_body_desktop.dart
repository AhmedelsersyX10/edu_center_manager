import 'package:flutter/material.dart';

class AttendanceViewBodyDesktop extends StatelessWidget {
  const AttendanceViewBodyDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: null);
  }
}
